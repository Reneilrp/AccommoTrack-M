import api, { normalizeResponse, normalizeError, normalizePaginatedResponse } from '../utils/api.js';

export { normalizeResponse, normalizeError, normalizePaginatedResponse };
export default api;
