<?php
// Let's do a curl to api.qrserver.com and see what we get, actually we can just check if urlencode is needed.
// Wait, curl can't read a QR code easily here without a library like zbarimg, but I can check if zbarimg is installed.
